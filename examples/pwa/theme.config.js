// A different client than the WordPress example: same components, different drivers.
export default {
  drivers: {
    color: { primary: '#6d28d9', accent: '#0d9488' },
    typography: { headingFamily: 'system-ui, sans-serif', bodyFamily: 'system-ui, sans-serif' },
    density: 0.9,
    radius: 12,
    style: { action: { radius: 'full', border: 'thin' } },
  },
};
